// Standalone /costsource route — renders the cost-ingest seam demo page.
import { CostSourcePage } from '@/costsource/CostSourcePage';

export default function CostSource() {
  return <CostSourcePage />;
}

