// Standalone /tokenomics route — renders the tokenomics integrity metrics page.
import { TokenomicsPage } from '@/tokenomics/TokenomicsPage';

export default function Tokenomics() {
  return <TokenomicsPage />;
}

