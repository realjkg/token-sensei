// Standalone /prediction route — renders the change-impact prediction demo page.
import { PredictionPage } from '@/prediction/PredictionPage';

export default function Prediction() {
  return <PredictionPage />;
}

